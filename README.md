# 🛡️ Terminal Safe System

Система безопасной авторизации через консоль, написанная на Python. 
Проект демонстрирует базовую логику защиты от атак методом перебора (Brute-force).

### ⚙️ Как это работает:
1. Система запрашивает 4-значный ПИН-код у пользователя.
2. Дается ровно **3 попытки** на ввод правильного пароля.
3. При каждом неверном вводе счетчик попыток уменьшается (срабатывает цикл `while`).
4. Если попытки исчерпаны — система имитирует блокировку пользователя и отключает сервер (команда `break` и `time.sleep`).

### 🚀 Технологии:
* **Язык:** Python 3.x
* **Ключевые концепции:** Условия (`if/else`), циклы (`while`), встроенная библиотека `time`.

### 💻 Как запустить на своем ПК:
1. Скачайте файл `safeterm.py`.
2. Откройте терминал в папке с файлом.
3. Запустите скрипт командой: `python safeterm.py`
4. *P.S. Правильный ПИН-код по умолчанию: 7777 (можно изменить в коде).*
